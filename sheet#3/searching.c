#include<stdio.h>

int main(){
    long long x;
    scanf("%lld", x);
    long long int n[x], y;
    scanf("%lld", y);
    for(int i = 0; i<x; i++){
        scanf("%lld",n[i]);
        if(n[i]==)

    }
}